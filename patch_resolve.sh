sed -i 's/AppAccessState.Expired/if (cached != null \&\& (!cached.isValidAt(now) || cached.status == "EXPIRED")) {\n                AppAccessState.Expired\n            } else if (subState is SubscriptionState.Loading) {\n                AppAccessState.Initializing\n            } else {\n                AppAccessState.Unentitled\n            }/g' app/src/main/java/com/example/billing/PlayEntitlementRepository.kt
