# Family Work Queues

These header-only files reserve reviewed workstreams. They intentionally contain no catalogue entries. Candidate data belongs in `../candidate-manifest.csv`.

Each family file will later record scope decisions, inclusions, exclusions, duplicate rulings, and reviewer notes before batch generation is authorised.
