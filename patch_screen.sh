sed -i 's/            Text(\n                text = "YOUR TRIAL HAS ENDED"/            val titleText = if (appAccessState is com.example.billing.AppAccessState.Expired) {\n                "YOUR TRIAL HAS ENDED"\n            } else {\n                "UNLOCK HUMAN STRENGTH"\n            }\n\n            val bodyText = if (appAccessState is com.example.billing.AppAccessState.Expired) {\n                "Your training history, logged workouts, and custom routines are safe. Continue training with Human Strength for £24\/year."\n            } else {\n                "Access all training modules, preserve local \& cloud sync data, and build your custom routines with Human Strength."\n            }\n\n            Text(\n                text = titleText/g' app/src/main/java/com/example/ui/screens/SubscriptionAccessScreen.kt
sed -i 's/                text = "Your training history, logged workouts, and custom routines are safe. Continue training with Human Strength for £24\/year."/                text = bodyText/g' app/src/main/java/com/example/ui/screens/SubscriptionAccessScreen.kt
